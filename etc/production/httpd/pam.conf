<VirtualHost *:80>
    ServerName pam.eea.europa.eu

    AddDefaultCharset UTF-8
    ProxyPreserveHost On
    RewriteEngine On

    #PAM uses node.js therefore we need a proxypass
    ProxyPass / http://localhost:3001 retry=0

</VirtualHost>
